const ENVIRONMENT = {
  URL_APP_API : import.meta.env.VITE_APP_API_URL 
} //tiene q llevarse si o si el import meta.env ya que asi se accede a las variables de entorno en vite
//asi como el vite delante de la variable 
export default ENVIRONMENT